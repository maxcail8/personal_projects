<script setup lang="ts">
import { ref } from 'vue'

// Reactive
const pageLoading = ref<boolean>(true)
// const cartsList = ref({})

// Computed

// onMounted(async () => {
//   await getCarts().then((response) => (cartsList.value = response.data))
// })
</script>

<template>
  <div></div>
</template>
